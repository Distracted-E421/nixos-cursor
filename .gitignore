# Public branches (pre-release, main) - restrictive
# Excludes entire .cursor/ directory

# Development artifacts
.cursor/

# Build artifacts
result
result-*
*.AppImage

# Nix build outputs
.direnv/
.envrc

# Editor artifacts
*.swp
*.swo
*~
.vscode/
.idea/

# OS artifacts
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.bak
*.orig

# Build artifacts
__pycache__/
*.pyc
target/
*.rlib
*.rmeta
*.so
*.d

# Cargo.lock - ignore for libraries, but keep for applications
Cargo.lock
# Exception: cursor-studio-egui is an application, needs reproducible builds
!cursor-studio-egui/Cargo.lock

cursor-studio/

# Screenshots - PRIVACY: May contain personal info, video calls, etc.
# Auto-cleaned after AI review - never commit these
screenshots/
!screenshots/.gitkeep

# AI Workspace - Contains AI thinking/drafts, not permanent code
# Scratchpad is gitignored, but structure files are committed
.ai-workspace/scratchpad/*.md
.ai-workspace/memory.db

# Payload database binaries (large, regeneratable)
tools/proxy-test/payload-db/**/requests/*.bin
tools/proxy-test/rust-captures/*.bin

# Python venv (should not be committed)
scripts/python/.venv/
**/.venv/
**/venv/
